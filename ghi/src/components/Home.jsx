import { useGetUserQuery } from "../app/apiSlice";
import { useSignoutUserMutation } from "../app/apiSlice";




const Home = () => {
    return (
                <h1>Hello</h1>
    );
};

export default Home;
